[project]
name = "classification-customer-churn"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"


# 1. RUNTIME DEPENDENCIES (These go into your final app)
dependencies = [
    "pandas>=3.0.2",
    "plotly>=6.9.0",
    "scikit-learn==1.8.0",
    "streamlit>=1.57.0",
    "xgboost-cpu>=3.2.0",
]

# 2. DEVELOPMENT DEPENDENCIES (These are left behind)
[dependency-groups]
dev = [
    "imblearn>=0.0",
    "ipykernel>=7.2.0",
    "matplotlib>=3.10.9",
    "scipy>=1.17.1",
    "seaborn>=0.13.2",
    "shap>=0.51.0",
]
